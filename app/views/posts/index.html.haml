- @posts.each do |post|
	= link_to (image_tag post.image.url(:small)), post
	%h1= link_to post.title, post
	%p= truncate(post.description, length: 20)
	%p
		= post.comments.count
		comments


%br/
=link_to "Add inspiration", new_post_path